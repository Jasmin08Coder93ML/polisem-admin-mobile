========================================================================
POLISEM PLATFORM - PROJECT ARCHITECTURE OVERVIEW
========================================================================

1. BACKEND (Go Language / Clean Architecture)
   ---------------------------------------------------------------------
   cmd/
     api/main.go          : Ulgamyň ýaýlym nokady (Entry point). Serveri işledýär.
   
   internal/              : Esasy biznes logika (Domain Driven Design).
     auth/                : Işgärleriň we dükan eýeleriniň hasaby (JWT, Sessions).
     shop/                : Dükan konfigurasiýalary (Dynamic Fields - azyk, gurluşyk).
     products/            : Haryt dolandyryşy (Elde girizmek, Barcode generation).
     sales/               : Bölek we lomaý satuw (POS - Point of Sale).
     warehouse/           : Ammar logistikasy (Galyndylary, ammarara hereketler).
     reports/             : Analitika moduly (Excel, Tax/Nalog hasabatlary).
     ai_copilot/          : AI beýnisi (Gemini API, Vision OCR, Voice Processing).

   pkg/                   : Global utilitler (GAÝTADAN ULANYLÝAN KOD).
     database/            : PostgreSQL birikmeleri we model gurluşlary.
     middleware/          : JWT we Howpsuzlyk süzgüçleri (Auth protection).

   .env                   : Gizlinlikler (API keys, DB connection strings).
   go.mod                 : Proýektiň kitaphanalary we wersiýalary (Dependency).

------------------------------------------------------------------------

2. FRONTEND (Flutter / Feature-Driven Architecture)
   ---------------------------------------------------------------------
   lib/
     core/                : Global konfigurasiýalar.
       app_theme.dart     : Reňkler, şriftler we global dizaýn (UI/UX).
       api_client.dart    : Backend bilen habarlaşýan HTTP (Dio) klient.
       app_constants.dart : Global sazlamalar we API adresleri.

     features/            : Her funksional aýratyn papka (Clean Code).
       auth/              : Login we hasaba alyş ekranlary.
       shop_setup/        : Dükan görnüşini saýlamak (Dinamik UI).
       product_management/: Haryt girizmek (Wizard algoritmi), Ştrihkod.
       pos_cashier/       : Kassa apparaty interfeýsi (Çalt satuw).
       reports_visual/    : Diagrammalar (FL Chart), Excel eksport düwmesi.
       tax_panel/         : Nalog we döwlet hasabatlarynyň görnüşi.
       ai_assistant/      : AI Chatbot, ses ýazgysy we kamera skanneri.

     main.dart            : Flutter applikasiýanyň başlanýan nokady (Entry point).
   
   pubspec.yaml           : Flutter paketleri we assetler.
   .env                   : Frontend konfigurasiýalary.

========================================================================
DESIGN PHILOSOPHY:
- Backend: Clean Architecture (Modullaryň biri-birine baglylygy ýok).
- Frontend: Feature-Driven (Her ekran özbaşdak, kodlar arassa).
- AI Integration: Modular (Gelejekde başga AI modullary aňsat goşulýar).
========================================================================
